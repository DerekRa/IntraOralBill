spring:
  config:
    import: optional:configserver:http://localhost:9296/config-server
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/DocMACCdbBill
    password: Qwerty.12345 #change this to your database username
    username: root #change this to your database password
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.springframework.orm.jpa.vendor.Database.MYSQL
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update # update or create
    properties:
      hibernate:
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

item:
  request:
    url:
      intraoralExamination: localhost:8085
